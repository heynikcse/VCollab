# VCollab frontend

See the [top-level README](../README.md) for full setup instructions
(Supabase project setup, environment variables, running the dev server).

Quick start once Supabase is configured:

```bash
npm install
npm run dev
```
